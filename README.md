# Smart Storage Service

Smart Storage Service is a service that provides a set of APIs for file storage and retrieval. It is built on top of the Spring Boot framework and uses PostgreSQL as the database.